//front-end javascript here

// wait for the document to be ready
$(function renderGame() {
    console.log("app.js is now running");

    var $GameCon = $("#gameCon");
    var gameHTML = $("#gameTemplate").html();
    // var gameTemp = _.template(gameHTML);

    // $gameContainer.empty();

    // $.get("/game").
//         done(function(data) {
//             console.log(data);
//             $(data).each(function (index, game) {
//                 var $game = $(gameTemp(game));
//                 $gameCon.append($game);
//             });
//         });

    

});
